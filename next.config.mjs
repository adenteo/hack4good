/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'placekitten.com',
      'source.unsplash.com',
      'images.unsplash.com',
      'unsplash.com',
    ],
  },
};

export default nextConfig;
